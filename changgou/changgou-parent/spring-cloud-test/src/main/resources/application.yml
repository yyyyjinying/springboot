server:
  port: 18060
spring:
  mail:
    host: smtp.qq.com
    username: 1020066395@qq.com
    password: gpizgnzvafdxbcca
    default-encoding: UTF-8
#    properties:
#      mail:
#        smtp:
#          ssl:
#            enable: true
  application:
    name: cloud-test
#  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://127.0.0.1:3306/changgou_goods?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
#    username: root
#    password: 123456
eureka:
  client:
#    registerWithEureka: false #表示是否注册自身到eureka服务器， 做集群是设置成true 生产配置为true
#    fetchRegistry: false #表示是否从eureka服务器获得注册信息，生产配置为true
#    registry-fetch-interval-seconds: 5 #默认为30s 生产使用缺省时间，测试环境修改小点
#    lease-renewal-interval-in-seconds: 5 #心跳时间，生产使用缺省时间，即服务续约间隔时间（缺省为30秒，生产使用缺省时间，测试环境修改小点）
#    lease-expiration-duration-in-seconds: 10 #发呆时间，即服务续约到期时间（缺省为90秒，生产使用缺省时间，测试环境修改小点）
    service-url:
      defaultZone: http://127.0.0.1:7001/eureka
  instance:
    prefer-ip-address: true
#hystrix 配置
hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 10000
          strategy: SEMAPHORE
#feign:
#  hystrix:
#    enabled: true


##发送邮件地址
mail:
  fromMail:
    sender: 1020066395@qq.com
    receiver: 1020066395@qq.com
